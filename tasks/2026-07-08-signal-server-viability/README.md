# signal-server-viability

Created: 2026-07-08

## Goal
Determine whether the official Signal server (`signalapp/Signal-Server`) can realistically
be self-hosted as a genuine standalone backend — our own infra, talked to by real Signal
clients, independent of Signal Foundation's own servers. Tracks scds-infra #148, a parallel
discovery track to #26 (see `tasks/2026-06-23-backup-comm-channel/`), which picked
self-hosted Matrix as the team's permanent backup channel specifically because it's
infrastructure-independent.

## External repos / dependencies
- (none) — pure research/writing task. Sources are external repos/articles read online, not
  cloned or built.

## How to run
Not applicable — no code to execute. `run.sh`/`main.py` are unused scaffold boilerplate, kept
for consistency with the task-folder convention (`make new-task`).

## Inputs
None.

## Outputs
None (pure research/writing task). Deliverable is `notes.md`, plus a draft comment for
scds-infra #148 (posted separately, after review — see notes.md).

# Notes — oil-wells-fake-dataset

- 2026-06-23: created. Copied generator + dataset from
  `cams_geospatial_platform` branch `orski/oil-wells-demo-dataset`:
  `tools/gen_oil_wells.py` → `main.py` (DEFAULT_OUTPUT_DIR changed to task-local
  `output/`); 6 dataset files copied verbatim into `dataset/`. Generator is pure
  stdlib (csv, struct, random) — no pip deps. Reference dataset committed under
  `dataset/`; `output/` is gitignored and regenerated by `./run.sh`.

- 2026-08-12: closed out scds#207's 3 remaining checklist items.
  - Extended `main.py` with `generate_lebanon_rows()` (offshore bbox, block-name
    fields, real-consortium operator names, deeper depth range, later discovery
    years, exploration-skewed status weights) behind `--regions`. Verified the
    Iraq-only default path is byte-identical to the pre-existing `dataset/`
    reference (CSV/SHP exact match, DBF exact match excl. the date header) —
    no regression to the original 75-well output.
  - Added `generate_production_series()` / `write_production_csv()` /
    `write_production_shapefile()` for the hourly production time-series
    deliverable: 43,830 hourly readings (5yr) for one well (`IQ-OW-001` by
    default), split into a companion CSV (keyed by well_id+timestamp) and a
    single-point shapefile carrying summary stats + a `prod_csv` pointer.
    Refactored `write_dbf`/`write_shapefile` to accept a custom field list
    (default unchanged) so the production shapefile could reuse the same
    writer code with its own schema.
  - `run.sh` now generates the combined demo dataset by default: 75 Iraq +
    12 Lebanon wells in one `OilWells` shapefile, plus `OilWellProduction`.
    `dataset/` was regenerated and updated to match.
  - Added `geoserver_workspace.yml`, matching the schema in
    `cams_geospatial_platform/src/workspaces.yml` (checked before writing —
    only the GeoServer-provisioning fields `create_workspaces_geoserver.py`
    actually reads, not the GeoNetwork/feature-catalogue sections).
  - Did NOT check off scds#207's GitLab checklist items or comment on the
    issue directly — per scds-infra's standing rule, that needs proposing
    the exact comment text back to the user first.

# Pillar data for the demo state. Edit values here (master-side), run
# `salt '*' saltutil.refresh_pillar`, then re-apply to push the change.
demo:
  owner: orski
  environment: lab
  message: "Provisioned by SaltStack 3008 LTS - saltstack-lab demo."
